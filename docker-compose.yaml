version: "3.7"

services:
  # detection:
  #   build: .

  disord-bot:
    build:  ./services/discord-bot
    restart: always
    volumes:
      - ./services/discord-bot/:/app
      - ./services/discord-bot/main.py:/app/main.py
    environment:
      PREFIX: "${PREFIX}"
      TOKEN: "${TOKEN}"
     